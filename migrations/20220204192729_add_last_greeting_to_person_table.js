/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
    return knex.schema.alterTable('person', function (table) {
        table.date('last_greeting').nullable().after('is_npc');
    })
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
    return knex.schema.alterTable('person', function (table) {
        table.dropColumn('last_greeting');
    })
};
